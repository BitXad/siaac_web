<?php
/* 
 * Generated by CRUDigniter v3.2 
 * www.crudigniter.com
 */
 
class Area_carrera extends CI_Controller{
    function __construct()
    {
        parent::__construct();
        $this->load->model('Area_carrera_model');
    } 

    /*
     * Listing of area_carrera
     */
    function index()
    {
        $data['area_carrera'] = $this->Area_carrera_model->get_all_area_carrera();
        
        $data['_view'] = 'area_carrera/index';
        $this->load->view('layouts/main',$data);
    }

    /*
     * Adding a new area_carrera
     */
    function add()
    {   
        $this->load->library('form_validation');

		$this->form_validation->set_rules('areacarrera_nombre','Areacarrera Nombre','trim|required', array('required' => 'Este Campo no debe ser vacio'));
		
		if($this->form_validation->run())     
        {   
            $params = array(
				'areacarrera_nombre' => $this->input->post('areacarrera_nombre'),
            );
            
            $area_carrera_id = $this->Area_carrera_model->add_area_carrera($params);
            redirect('area_carrera/index');
        }
        else
        {            
            $data['_view'] = 'area_carrera/add';
            $this->load->view('layouts/main',$data);
        }
    }  

    /*
     * Editing a area_carrera
     */
    function edit($areacarrera_id)
    {   
        // check if the area_carrera exists before trying to edit it
        $data['area_carrera'] = $this->Area_carrera_model->get_area_carrera($areacarrera_id);
        
        if(isset($data['area_carrera']['areacarrera_id']))
        {
            $this->load->library('form_validation');

			$this->form_validation->set_rules('areacarrera_nombre','Areacarrera Nombre','trim|required', array('required' => 'Este Campo no debe ser vacio'));
		
			if($this->form_validation->run())     
            {   
                $params = array(
					'areacarrera_nombre' => $this->input->post('areacarrera_nombre'),
                );

                $this->Area_carrera_model->update_area_carrera($areacarrera_id,$params);            
                redirect('area_carrera/index');
            }
            else
            {
                $data['_view'] = 'area_carrera/edit';
                $this->load->view('layouts/main',$data);
            }
        }
        else
            show_error('The area_carrera you are trying to edit does not exist.');
    } 

    /*
     * Deleting area_carrera
     */
    function remove($areacarrera_id)
    {
        $area_carrera = $this->Area_carrera_model->get_area_carrera($areacarrera_id);

        // check if the area_carrera exists before trying to delete it
        if(isset($area_carrera['areacarrera_id']))
        {
            $this->Area_carrera_model->delete_area_carrera($areacarrera_id);
            redirect('area_carrera/index');
        }
        else
            show_error('The area_carrera you are trying to delete does not exist.');
    }
    
}
